// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.snektracker"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
